
upstream zelthy {
    server app:8000;
}

server {

    listen 1443;
    client_max_body_size 4G;

    location / {
        proxy_pass http://zelthy;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
    location /static/ {
        alias /zelthy/${PROJECT_NAME}/static/;
    }

}
