---
sidebar_label: Linking Crudview
---

### Configuration
- Define a Workflow class inheriting from `WorkflowBase`.
- Specify status transitions, transition conditions, and processing methods within the Workflow class.

## Linking Workflow to CRUD View

In this final section, we illustrate how to link the Workflow Package to a CRUD View for seamless integration within the project.

### Linking Process
- Import the Workflow class into the CRUD View module.
- Attach the Workflow class to the CRUD View.

By organizing the content into these sections, readers can easily grasp the purpose, features, installation process, and integration steps of the Workflow Package within their projects.

