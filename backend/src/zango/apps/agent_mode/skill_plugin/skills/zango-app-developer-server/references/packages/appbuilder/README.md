# AppBuilder package references

Routes and menus — how a page becomes reachable in the app's navigation.

| Doing | Read |
|---|---|
| Registering routes and menus via the API | `api-configuration.md` |
| Understanding what AppBuilder provides | `overview.md` |

Route and menu registration is mandatory for every page — a view with no route
never appears in navigation. Read `api-configuration.md` at the point you
register, in STEP 5h.
